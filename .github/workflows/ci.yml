name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  go:
    uses: agoodkind/go-makefile/.github/workflows/_ci.yml@main
    permissions:
      contents: read
      id-token: write
